Pour créer un consumer kafka il ne faut les dépendences suivantes :
    -   Apache Kafka 3.4.0
    -   Kafka-clients
Pour checker la version kafka installée sur la machine
<code>kafka-topics --version<code>